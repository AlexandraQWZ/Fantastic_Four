<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />

    <!-- RESPONSIVE -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- GOOGLE FONT -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Akshar:wght@100;200;300;400;500;600;700&display=swap"
      rel="stylesheet"
    />

    <!-- STYLE -->
    <!-- <link rel="stylesheet" href="/style.css" /> -->

    <!-- ICONS -->
    <script src="https://kit.fontawesome.com/ec41095585.js" crossorigin="anonymous"></script>

    <!-- TITLE -->
    <title>Fantastic Four – Edit Question</title>

    <!-- INLINE OVERRIDES FOR NEON EDIT FORM -->
    <style>
      .edit-form-container {
        max-width: 850px;
        margin: 70px auto;
        padding: 30px;
        background: rgba(0, 20, 30, 0.6);
        border-radius: 20px;
        backdrop-filter: blur(10px);
        box-shadow: 0 0 30px rgba(0, 229, 255, 0.2);
        border: 1px solid rgba(0, 229, 255, 0.15);
      }

      .edit-header {
        text-align: center;
        margin-bottom: 40px;
      }

      .edit-header h4 {
        color: #00e5ff;
        text-shadow: 0 0 15px #00e5ff;
      }

      .edit-form {
        padding: 25px;
      }

      .form-group label {
        display: block;
        margin-bottom: 8px;
        font-weight: 600;
        color: #aef8ff;
      }

      .form-group input,
      .form-group textarea {
        width: 100%;
        padding: 15px;
        background: rgba(0, 25, 40, 0.7);
        border: 1px solid rgba(0, 229, 255, 0.3);
        border-radius: 15px;
        font-size: 1.1rem;
        font-family: 'Akshar', sans-serif;
        color: #eaffff;
        box-shadow: 0 0 15px rgba(0, 229, 255, 0.1);
      }

      .form-group input:focus,
      .form-group textarea:focus {
        outline: none;
        border-color: #00e5ff;
        box-shadow: 0 0 25px rgba(0, 229, 255, 0.5);
      }

      .form-group textarea {
        height: 180px;
        resize: none;
      }

      .form-buttons {
        display: flex;
        gap: 20px;
        margin-top: 30px;
      }

      /* Buttons */
      .btn {
        flex: 1;
        padding: 14px 30px;
        border-radius: 20px;
        font-size: 1.1rem;
        cursor: pointer;
        border: none;
        text-align: center;
        transition: 0.2s ease;
        font-family: 'Akshar', sans-serif;
      }

      .btn-confirm {
        background: rgba(0, 229, 255, 0.25);
        color: #e6faff;
        border: 1px solid rgba(0, 229, 255, 0.5);
        box-shadow: 0 0 15px rgba(0, 229, 255, 0.25);
      }

      .btn-confirm:hover {
        background: rgba(0, 229, 255, 0.4);
        box-shadow: 0 0 30px rgba(0, 229, 255, 0.5);
      }

      .btn-cancel {
        background: rgba(255, 60, 60, 0.25);
        color: #ffecec;
        border: 1px solid rgba(255, 80, 80, 0.5);
        box-shadow: 0 0 15px rgba(255, 80, 80, 0.3);
      }

      .btn-cancel:hover {
        background: rgba(255, 60, 60, 0.4);
        box-shadow: 0 0 30px rgba(255, 80, 80, 0.5);
      }

    </style>
  </head>

  <body>
    <!-- ********* HEADER ********* -->
    <div class="container-header">
      <header>
        <nav class="nav-edit">
          <a href="/">
            <img src="/img/footer-icon.svg" class="footer-logo" alt="Fantastic Four logo" />
          </a>

          <a href="https://github.com/AlexandraQWZ/Fantastic_Four" target="_blank">
            <img src="/img/Github@2x.svg" class="logo" alt="Github logo" />
          </a>
        </nav>
      </header>
    </div>

    <!-- ********* EDIT FORM SECTION ********* -->
    <section class="edit-form-container">
      <div class="edit-header">
        <h4 class="about-why">EDIT YOUR QUESTION</h4>
      </div>

      <form action="/update/<%= question.id %>" method="POST" class="edit-form">
        
        <div class="form-group">
          <label for="category">Category:</label>
          <input
            type="text"
            id="category"
            name="category"
            value="<%= question.category %>"
            required
          />
        </div>

        <div class="form-group">
          <label for="content">Question Content:</label>
          <textarea
            id="content"
            name="content"
            required
          ><%= question.content %></textarea>
        </div>

        <div class="form-buttons">
          <button type="submit" class="btn btn-confirm">
            <i class="fas fa-check"></i> Save Changes
          </button>

          <a href="/" class="btn btn-cancel">
            <i class="fas fa-times"></i> Cancel
          </a>
        </div>

      </form>
    </section>

    <!-- ********* FOOTER ********* -->
    <footer>
      <section class="footer-links">
        <ul class="links">
          <a href="https://github.com/AlexandraQWZ" target="_blank">// GITHUB</a>
          <a href="/">// PORTFOLIO</a>
          <a href="/">// CONTACT</a>
        </ul>
      </section>

      <div class="footer-title">
        <a href="/">
          <img src="/img/footer-icon.svg" class="footer-logo" alt="Fantastic Four footer logo" />
        </a>
      </div>
    </footer>

  </body>
</html>
